# Session Handoff: BPCL Overlay Transitions

## What We Just Did
- **Release Automation**: Bumped the GitHub release wait time in the `bpcl-release` skill from 180s to 240s (4 minutes).
- **Hero Image Adjustments**: Zoomed the draft pick hero image by 20% (scale `1.35` -> `1.55`) and moved it up 30% (`-translate-y-[30%]`) in `apps/overlay-web/src/components/draft/DraftHeroMedia.tsx`.
- **GSI Automation Logs**: Fixed a typo in `apps/broadcast-api/src/gsi/routes.ts` so the draft done log now correctly says `Game in 45s` instead of 25s.
- **Hero Focus Logic**: `kdaCard` and `livePlayerCard` are now strictly tied to whether a player is focused. If there's no focus, both cards are forcefully hidden.
- **GSI Heartbeat Logic**: The GSI heartbeat no longer force-hides overlays. It only marks `gsiConnected: false`. The overlays remain in their current state until a new state is broadcast.
- **Released v1.1.14**: Successfully built and published the newest updates via the Desktop Launcher.

## The Outstanding Issue
**Bug**: The Versus screen does not show automatically 5 seconds after the Draft finishes.
- **Symptoms**: The timer fires successfully in `routes.ts`, and no "timers cleared" log is produced. However, the Versus screen still does not display in the OBS Browser Source.
- **Things to investigate**:
  - We ruled out aggressive caching (after clearing the OBS cache manually).
  - The API does update the state correctly (`overlayVisibility.versus = "visible", draft = "hidden"`).
  - Could the frontend `OverlayContainer` or `VersusPage` be failing to render based on that state? 
  - Is there a race condition with Socket.IO when the client tries to re-fetch the state?
  - Does the client receive the `broadcastFull` event when the 5s timer triggers?

## Next Steps for the New Agent
1. Trace the `broadcastFull` payload from `globalVersusTimeout` in `routes.ts` down to the `overlay-web` frontend.
2. Check `apps/overlay-web/src/App.tsx` or `OverlayContainer` to see how `overlayVisibility.versus === "visible"` is handled.
3. Check if there are any frontend transition blocks or errors in the OBS browser console.
