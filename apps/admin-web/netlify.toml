# Admin site — Netlify cwd is usually repo root (no base dir). If base is apps/admin-web, use cd ../.. in command.

[build]
  command = "npm ci && npm run build:admin"
  publish = "apps/admin-web/dist"
