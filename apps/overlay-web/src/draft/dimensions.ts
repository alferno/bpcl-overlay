/** Broadcast draft scale — 1920×1080 */
export const DRAFT_PICK_HEIGHT = 236;
/** Ban tile width as fraction of each grid cell (0.8 = 20% smaller than full cell). */
export const DRAFT_BAN_TILE_SCALE = 0.8;
